<ContentDialog
    x:Class="DocsUnoTesting.Presentation.CreateTestResultDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Create Test Result"
    PrimaryButtonText="Create"
    SecondaryButtonText="Cancel">

    <StackPanel Spacing="16">
        <ComboBox Header="Student"
                  ItemsSource="{Binding Students}"
                  DisplayMemberPath="FullName"
                  SelectedItem="{Binding SelectedStudent, Mode=TwoWay}" />
        <ComboBox Header="Test"
                  ItemsSource="{Binding Tests}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedTest, Mode=TwoWay}" />
        <TextBox Header="Score"
                 Text="{Binding Score, Mode=TwoWay}" />
    </StackPanel>
</ContentDialog>
