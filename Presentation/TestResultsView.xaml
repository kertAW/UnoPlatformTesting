<UserControl
    x:Class="DocsUnoTesting.Presentation.TestResultsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="self"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <ListView
            Grid.Row="0"
            Grid.Column="1"
            ItemsSource="{Binding TestResults}"
            Width="800"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
        >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0,10" />
                    <Setter Property="BorderBrush" Value="LightGray" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Student" FontWeight="Bold" />
                        <TextBlock Grid.Column="1" Text="Test" FontWeight="Bold" />
                        <TextBlock Grid.Column="2" Text="Score" FontWeight="Bold" />
                        <TextBlock Grid.Column="3" Text="Actions" FontWeight="Bold" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="{Binding Student.FullName}"
                            VerticalAlignment="Center"
                        />
                        <TextBlock
                            Grid.Column="1"
                            Text="{Binding PassedTest.Name}"
                            VerticalAlignment="Center"
                        />
                        <TextBlock
                            Grid.Column="2"
                            Text="{Binding Score}"
                            VerticalAlignment="Center"
                        />
                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                            <Button Background="Transparent" BorderThickness="0">
                                <SymbolIcon Symbol="Edit" />
                            </Button>
                            <Button Background="Transparent" BorderThickness="0">
                                <SymbolIcon Symbol="Delete" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button
            Grid.Row="1"
            Command="{Binding CreateTestResultCommand}"
            CommandParameter="{Binding XamlRoot, ElementName=self}"
            HorizontalAlignment="Center"
            Margin="10"
        >
            <SymbolIcon Symbol="Add" />
        </Button>
    </Grid>
</UserControl>
