<Page
    x:Class="DocsUnoTesting.Presentation.TestResultDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <StackPanel Spacing="10">
            <TextBlock Text="Test Result Details" FontSize="24" FontWeight="Bold" />
            <TextBlock Text="{Binding TestResult.Student.FullName, FallbackValue='Student:'}" />
            <TextBlock Text="{Binding TestResult.PassedTest.Name, FallbackValue='Test:'}" />
            <TextBlock Text="{Binding TestResult.Score, FallbackValue='Score:'}" />
        </StackPanel>
    </Grid>
</Page>
